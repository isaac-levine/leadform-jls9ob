# Application Environment
# Required: Specify the application environment (development, staging, production)
NODE_ENV=development

# API Configuration
# Required: Base URL for backend API endpoints
# Production: Use HTTPS URL with valid SSL certificate
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# WebSocket Configuration
# Required: WebSocket server URL for real-time messaging
# Production: Use WSS URL with valid SSL certificate
NEXT_PUBLIC_SOCKET_URL=ws://localhost:3000

# Application URLs
# Required: Public URL of the web application
# Production: Use HTTPS URL with valid SSL certificate
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication Configuration
# Required: NextAuth.js callback URL
# Must match NEXT_PUBLIC_APP_URL in production
NEXTAUTH_URL=http://localhost:3000

# Security Configuration
# Required: Generate a secure random string (min 32 chars)
# Command: openssl rand -base64 32
# WARNING: Never commit this value to version control
NEXTAUTH_SECRET=your-nextauth-secret-key

# Error Tracking Configuration
# Optional: Sentry DSN for error reporting
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn

# Rate Limiting Configuration
# Optional: API rate limit (requests per minute)
NEXT_PUBLIC_API_RATE_LIMIT=100

# WebSocket Configuration
# Optional: Maximum reconnection attempts
NEXT_PUBLIC_SOCKET_RECONNECT_ATTEMPTS=3